extends default

block head

block content
  .post-list
    h1.title
      a(href="/blog/archives") Archives
    each post in posts
      .post-list-item
        .post-list-item__date= post.date.split(' ')[0] + ' '
        .post-list-item__title
          a(href=post.url)= post.title
